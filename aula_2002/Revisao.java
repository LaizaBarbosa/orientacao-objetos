import java.util.Scanner;

public class Revisao {
    public static void main(String[] args) {
        // (soma*1.)/i -> converte corretamente um numero dividido por int para double, atribuindo a casa decimal e tals
        // Scanner scan = new Scanner(System.in);
        // int opcao = 1, soma = 0, n = 0, i = 0;
        
        // while (opcao != 0) {
        //     System.out.print("valor? ");
        //     n = scan.nextInt();

        //     soma+=n;
        //     i++;

        //     System.out.println("Continua? ");
        //     opcao = scan.nextInt();    
        // }
        // System.out.println(soma);
        // System.out.println(i);

        // for (int i = 0; i < 10; i++) {
        //     int dado = (int) (Math.random()*15)+20; //ele soma com o valor maximo - valor maximo q vc quer menos o minimo
        // }
        
        double valor = Math.pow(2, 1./1000);
            System.out.println(valor);
    }
}
